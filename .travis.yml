sudo: required
language: java
install: true

services:
  - docker

before_install:
  - docker build . --tag pgr301 --build-arg JAR_FILE=./build/libs/

script:
  - chmod +x gradlew
  - ./gradlew build
  - docker ps -a




